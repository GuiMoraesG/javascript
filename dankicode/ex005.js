const nome = 'GUILHERME'
const nomeReformulado = nome.toLowerCase()

if(nomeReformulado == 'guilherme') {
    console.log(`Olá, ${nomeReformulado}`)
} else {
    console.log(`O nome ${nomeReformulado} não é conpativel`)
}